# 项目计划与执行清单

- 项目主线：**三个业务判断**（[docs/三个业务判断.md](../docs/三个业务判断.md)）——所有产出往判断上靠：先有判断，管道只是取证
- 工作原则：① 口径先行——每个指标先写清分子/分母/粒度/过滤再动手 ② 关键数字用两种独立工具交叉核对 ③ 假设与数据边界显式写出
- 过程与决策记录见 [项目日志](项目日志.md)

## 环境 + 数据认知

### D1｜搭环境、拿数据、读懂业务
- [x] 环境准备：Python（jupyterlab / sqlalchemy / pymysql / seaborn）+ MySQL 8.0 + Power BI + Git
- [x] 数据集下载解压到 `data\raw\`
- [x] `pd.read_csv('...', encoding='latin-1')` 读入（⚠️ 不是 UTF-8，第一个坑）；跑 `df.shape / df.info() / df.head()`
- [x] 对照字段说明补全 `docs\字段清单.md`：每字段标 保留/删除/存疑
- [x] 验收：一句话说清数据粒度 =「订单行 order item」；核心字段业务含义逐一确认

## SQL + 数据入库建模

### D2-D3｜SQL 与数据入库
- [x] MySQL 练习库过核心语法：聚合 / GROUP BY / HAVING / JOIN / CASE WHEN / 子查询 / 窗口函数累计占比（D10 帕累托要用）
- [x] pandas 最小清洗：删无用列、列名统一 snake_case、两个日期字段 parse 成 datetime
- [x] SQLAlchemy `to_sql` 写入 MySQL staging 表（chunksize 分块；库字符集 utf8mb4）
- [x] 验收：写 COUNT 验证 staging 行数 = CSV 行数

### D4｜星型模型
- [x] 画数据模型图：fact_order_item + dim_customer / dim_product / dim_date
- [x] `CREATE TABLE ... AS SELECT DISTINCT ...` 从 staging 拆维表，再建事实表、设主外键
- [x] 写 5 条校验 SQL：各表行数、关键字段空值率、**用 real > scheduled 重算迟发标记，核对自带 Late_delivery_risk 是否一致**
- [x] 产出 `sql\schema.sql` + 模型图，校验全过
- [x] 验收：能答「为什么不用一张宽表打天下」（冗余、口径一致性、BI 性能）

## pandas 分析 + 指标口径

### D5｜KPI 口径定义
- [x] 写 `docs\kpi_definitions.md`：准时交付率、迟发率、平均履约周期、销售额、利润率、亏损单占比——每个写清分子分母
- [x] 口径决策：Shipping canceled 的订单算不算分母？写下理由
- [x] 验收：「按月×运输方式的迟发率」用 SQL 和 pandas 各算一遍，数字对得上

### D6｜EDA 探索
- [x] 整体迟发率 → 按 Shipping Mode / Market / 品类 / 月份逐维切分（判断②素材）
- [x] 销售利润分布、负利润订单占比 → 下钻到 品类/SKU×区域 组合，注意折扣率与亏损的关系（判断①素材）
- [x] 每张图下配一句话结论
- [x] 产出 `notebooks\01_eda.ipynb` + 候选洞察清单：每条按「现象 + 初步量化 + 值得追问什么」写，往三个判断上靠

## Power BI 看板

### D7-D8｜看板页 1：履约总览
- [x] 星型模型建关系（CSV 导入）；`_度量值` 表集中管理度量值
- [x] KPI 卡片 + 月度趋势线 + 运输方式对比 + Market 图 + 年月/市场/品类切片器
- [x] 页面要回答的决策问题写进标题/副标题——读者 30 秒拿到答案
- [x] 验收：切片器联动正确；看板数字与 pandas 结果交叉核对一致

### D9｜看板页 2：盈利结构 + 打磨
- [x] 页面回答一个决策问题，那句带数字的判断放页面最显眼处
- [x] 统一配色、标题；导出高清截图 → `dashboard\`
- [x] 验收：3 分钟讲完，听者能复述出**那句判断**和至少 1 条支撑证据（讲不通 = 设计有问题）

## 两个深度分析

### D10｜迟发根因（一）：帕累托 + 交叉下钻
- [x] 窗口函数算累计占比：找出「XX% 的迟发集中在哪些 运输方式×市场×品类 组合」
- [x] 对比不同承诺天数（scheduled）下的实际达成率
- [x] 验收：输出一句带数字的定位结论，并算出这批迟发订单对应的销售额（给判断②的"值多少钱"打底）

### D11｜迟发根因（二）：量化建议
- [x] 情景测算：调整承诺天数 → 基于历史数据反事实推演迟发率 A%→B%
- [x] **写清楚假设和局限**（历史数据推演，不是因果实验；数据无罚金/流失字段，金额影响用受影响订单 GMV 表述）
- [x] 产出 `notebooks\02_late_delivery_analysis.ipynb` + **判断②完整句**：迟发集中在哪、影响多少金额订单、建议什么动作、预计降到多少

### D12｜ABC-XYZ 分级 + 安全库存
- [x] SKU 级：ABC 按销售额累计占比分（80/15/5）；XYZ 按周需求变异系数 CV 分（<0.5 / 0.5-1 / >1，在售窗口内补零口径）
- [x] 画九宫格；对典型格给差异化库存策略
- [x] 用 `SS = z×σ×√L` 测算安全库存
- [x] **判断③资金账**：「一刀切高服务水平」vs「ABC-XYZ 差异化服务水平」两种策略的安全库存资金占用对比（数据无库存快照，这是策略对比测算——提前期/服务水平/成本口径的假设逐条写明）
- [x] 验收：讲清 XYZ 为什么用 CV 而不是 σ（消除量纲、跨 SKU 可比）；讲清判断③的每条假设

## 交付整理

### D13｜GitHub + README + 一页报告
- [x] README：**三个业务判断原文（现象+量化影响+建议动作）放最前面**，然后才是数据说明、模型图、口径、复现步骤
- [x] 一页 PDF 结论报告 → `report\`
- [ ] 建 repo；原始数据不上传（README 放 Kaggle 链接）
- [ ] 验收：陌生人按 README 能复现；README 里每个数字都有出处

## 可选扩展
- [ ] sklearn 逻辑回归预测迟发风险：train/test split、AUC、特征重要性
